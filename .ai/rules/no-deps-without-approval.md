---
glob: '**/*.{php,vue,js}'
title: 'No instalar dependencias sin aprobar'
---

Nunca ejecutar `composer require` / `composer remove` / `npm install` / `npm uninstall` sin autorización explícita del usuario. Aplica a TODOS los proyectos.
