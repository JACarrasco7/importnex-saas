<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Ziggy Base URL
    |--------------------------------------------------------------------------
    |
    | The base URL for your application. This is used to generate absolute
    | URLs for your application routes. It defaults to the APP_URL environment
    | variable, but you can override it here if needed.
    |
    */

    'url' => env('APP_URL'),

    /*
    |--------------------------------------------------------------------------
    | Ziggy Base Domain
    |--------------------------------------------------------------------------
    |
    | The base domain for your application. This is used to generate absolute
    | URLs for your application routes that include the domain. It defaults to
    | the APP_URL environment variable, but you can override it here if needed.
    |
    */

    'domain' => env('APP_URL'),
];
